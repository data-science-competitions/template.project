---
output: github_document
---

```{r setup, echo = FALSE, results = 'hide', message = FALSE, warning = FALSE}
source("./.app/README/helpers.R")
knitr::opts_chunk$set(
  echo = TRUE, 
  message = FALSE,
  warning = FALSE,
  fig.align = "center", 
  out.width = "100%"
)

devtools::load_all(README$getwd())
DESCRIPTION <- desc::description$new(file.path(README$getwd(), "DESCRIPTION"))
```

```{r badges, echo = FALSE}
glue <- stringr::str_glue
badges <- list()

badges$CICD <- list(
  badge_name = "continuous-integration", 
  href = glue("https://travis-ci.org/{slug}", slug = tic::ci_get_slug()), 
  src = glue("https://travis-ci.org/{slug}.svg?branch={branch}", slug = tic::ci_get_slug(), branch = "master")
)
badges$covr <- list(
  badge_name = "code-coverage", 
  href = glue("https://codecov.io/github/{slug}/?branch={branch}", slug = tic::ci_get_slug(), branch = "master"), 
  src = glue("https://codecov.io/gh/{slug}/branch/{branch}/graph/badge.svg", slug = tic::ci_get_slug(), branch = "master")
)
invisible(lapply(badges, do.call, what = usethis::use_badge))
```

# ``r DESCRIPTION$get_field("Package")`` <img src='https://i.imgur.com/cLcAYfz.png' align="right" height="50"/>

<!-- badges: start -->
[![continuous-integration](https://travis-ci.org/data-science-competitions/template.project.svg?branch=master)](https://travis-ci.org/data-science-competitions/template.project)
[![code-coverage](https://codecov.io/gh/data-science-competitions/template.project/branch/master/graph/badge.svg)](https://codecov.io/github/data-science-competitions/template.project/?branch=master)
<!-- badges: end -->

`r DESCRIPTION$get_field("Title")`
---

```{r, echo = FALSE, out.width = "75%"}
knitr::include_graphics("https://i.imgur.com/RLEQkhe.png")
```

## Overview

`r DESCRIPTION$get_field("Description")`

## Installation

You can install ``r DESCRIPTION$get_field("Package")`` by using:

```
install.packages("remotes")
remotes::install_github("`r DESCRIPTION$get_field("Package")`")
```
