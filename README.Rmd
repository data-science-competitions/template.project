---
output: github_document
---

```{r setup, echo = FALSE, results = 'hide', message = FALSE, warning = FALSE}
source("./.dev/README/helpers.R")
knitr::opts_chunk$set(
  echo = TRUE, 
  message = FALSE,
  warning = FALSE,
  fig.align = "center", 
  out.width = "100%"
)

devtools::load_all(README$getwd())
DESCRIPTION <- desc::description$new(file.path(README$getwd(), "DESCRIPTION"))
```

```{r badges, echo = FALSE}
glue <- stringr::str_glue
slug <- tryCatch(tic::ci_get_slug(), error = function(e) "")
branch = "master"

badges <- list()

badges$CICD <- list(
  badge_name = "continuous-integration", 
  href = glue("https://github.com/{slug}/actions", slug = slug), 
  src = glue("https://codecov.io/github/{slug}/workflows/tic/badge.svg?branch={branch}", slug = slug, branch = branch)
)
badges$covr <- list(
  badge_name = "code-coverage", 
  href = glue("https://codecov.io/github/{slug}/?branch={branch}", slug = slug, branch = branch), 
  src = glue("https://codecov.io/gh/{slug}/branch/{branch}/graph/badge.svg", slug = slug, branch = branch)
)
invisible(lapply(badges, do.call, what = usethis::use_badge))
```

# ``r DESCRIPTION$get_field("Package")`` <img src='https://i.imgur.com/cLcAYfz.png' align="right" height="50"/>

<!-- badges: start -->
[![continuous-integration](https://codecov.io/github/data-science-competitions/template.project/workflows/tic/badge.svg?branch=master)](https://github.com/data-science-competitions/template.project/actions)
[![code-coverage](https://codecov.io/gh/data-science-competitions/template.project/branch/master/graph/badge.svg)](https://codecov.io/github/data-science-competitions/template.project/?branch=master)
<!-- badges: end -->

`r DESCRIPTION$get_field("Title")`
---

```{r, echo = FALSE, out.width = "75%"}
knitr::include_graphics("https://i.imgur.com/RLEQkhe.png")
```

## Overview

`r DESCRIPTION$get_field("Description")`

## Installation

You can install ``r DESCRIPTION$get_field("Package")`` by using:

```
install.packages("remotes")
remotes::install_local("`r DESCRIPTION$get_field("Package")`")
```
